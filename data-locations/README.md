Data locations
============================

This folder contains a single comma-separated text file `locations.csv` that 
contains the location name, FIPS code, and population (taken from the JHU CSSE 
GitHub repository) for each valid forecast location.